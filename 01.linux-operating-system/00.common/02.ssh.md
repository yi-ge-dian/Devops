1. 生成 ssh 密钥对

```bash
ssh-keygen -t rsa -b 4096
# 一路回车即可
# 生成的文件在当前用户的 .ssh 目录下
```

2. 在远程服务器上将公钥添加到 authorized_keys 文件中
```bash
# 本机执行
cat ~/.ssh/id_rsa.pub | ssh user@host 'cat >> ~/.ssh/authorized_keys'
# 或
# 本机执行
cat ~/.ssh/id_rsa.pub
# 远程服务器对应用户目录下执行，添加进去
vim ~/.ssh/authorized_keys
```
